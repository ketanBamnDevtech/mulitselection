import React, { Fragment } from 'react'
import AccordionComponent from './AccordionComponent';

const Tabs: React.FC = () => {
	return (
		<Fragment>
			<AccordionComponent/>
		</Fragment>
	)
}

export default Tabs;
